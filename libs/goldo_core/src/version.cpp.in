#include "goldobot/version.hpp"

namespace goldobot {
  const char* c_git_commit_id = "${GOLDO_GIT_COMMIT_ID}";
  const char* c_git_commit_short_id = "${GOLDO_GIT_COMMIT_SHORT_ID}";
  const char* c_git_commit_date = "${GOLDO_GIT_COMMIT_DATE}";
  const char* c_git_branch = "${GOLDO_GIT_BRANCH}";
}  // namespace goldobot
